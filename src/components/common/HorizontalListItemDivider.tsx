import React, { FC, memo } from 'react';
import { View } from 'react-native';

const HorizontalListItemDivider = memo(() => {
  return <View className='mr-2'></View>;
});

export default HorizontalListItemDivider;