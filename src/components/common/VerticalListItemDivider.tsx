import React, { FC, memo } from 'react';
import { View } from 'react-native';

const VerticalListItemDivider = memo(() => {
  return <View className="mb-2"></View>;
});

export default VerticalListItemDivider;
