import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
btnContainer: {
    borderRadius: 180,
  },
  btnIcon: {
    height: 35,
    width: 35,
  },
});

export default styles;
